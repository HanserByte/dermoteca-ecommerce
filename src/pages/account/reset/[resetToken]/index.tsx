import { useRouter } from "next/router";
import React from "react";

const ResetAccount = ({ props }) => {
  const router = useRouter();
  return <div>ResetAccount</div>;
};

export default ResetAccount;
